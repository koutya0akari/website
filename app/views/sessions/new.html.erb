<% content_for :title, 'ログイン' %>

<section class="section">
  <div class="section-header">
    <span class="section-eyebrow">Account</span>
    <h2>ログイン</h2>
    <p>日記を保存するにはログインしてください。</p>
  </div>

  <div class="split-grid">
    <article class="profile-card diary-editor">
      <% if static_export? %>
        <div class="diary-login-callout">
          <p>GitHub Pages の静的バージョンではログイン機能を利用できません。</p>
          <p>学習日記は <%= link_to 'Diary ページ', diary_path, class: 'resource-link' %> から閲覧してください。</p>
        </div>
      <% else %>
        <%= form_with url: login_path, scope: :session, class: 'diary-form' do |form| %>
          <label class="diary-label">
            <span>メールアドレス</span>
            <%= form.email_field :email, required: true, autofocus: true, class: 'diary-input', autocomplete: 'email' %>
          </label>
          <label class="diary-label">
            <span>パスワード</span>
            <%= form.password_field :password, required: true, class: 'diary-input', autocomplete: 'current-password' %>
          </label>
          <div class="diary-actions">
            <%= form.submit 'ログイン', class: 'btn btn-primary' %>
            <%= link_to 'ホームに戻る', root_path, class: 'btn btn-outline' %>
          </div>
        <% end %>
      <% end %>
    </article>
  </div>
</section>
