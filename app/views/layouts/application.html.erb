<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><%= page_title(content_for?(:title) ? yield(:title) : nil) %></title>
    <meta name="description" content="大学で数学を学ぶ学生ユウのポートフォリオ。研究プロジェクト、学習ノート、イベント登壇、アウトプットをまとめた GitHub Pages 向けウェブサイト。">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', 'data-turbo-track': 'reload', media: 'all' %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <nav class="navbar">
      <div class="container">
        <%= link_to t('math_website.title'), root_path, class: 'brand' %>
        <div class="nav-links">
          <%= nav_link(:home, root_path) %>
          <%= nav_link(:research, research_path) %>
          <%= nav_link(:teaching, teaching_path) %>
          <%= nav_link(:talks, talks_path) %>
          <%= nav_link(:publications, publications_path) %>
          <%= nav_link(:contact, contact_path) %>
        </div>
      </div>
    </nav>

    <main>
      <%= yield %>
    </main>

    <footer class="footer">
      <p>&copy; <%= Time.current.year %> <%= t('math_website.title') %></p>
      <p>
        <%= link_to 'GitHub', 'https://github.com', target: '_blank', rel: 'noopener' %>
        <%= link_to 'Zenn', 'https://zenn.dev', target: '_blank', rel: 'noopener' %>
        <%= link_to 'Speaker Deck', 'https://speakerdeck.com', target: '_blank', rel: 'noopener' %>
      </p>
    </footer>
  </body>
</html>
