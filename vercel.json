{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "pnpm build",
  "outputDirectory": ".next",
  "git": {
    "deploymentEnabled": {
      "main": false,
      "production": true,
      "deploy": true
    }
  },
  "ignoreCommand": "if [ \"$VERCEL_GIT_COMMIT_REF\" != \"production\" ] && [ \"$VERCEL_GIT_COMMIT_REF\" != \"deploy\" ]; then exit 0; else exit 1; fi"
}
